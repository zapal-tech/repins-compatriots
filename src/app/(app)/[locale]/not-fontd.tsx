import { MessageScreen, MessageScreenButtonProps } from '@app/components/MessageScreen';

const NotFound = async () => <MessageScreen title="404" description="Сторінку не знайдено" />;

export default NotFound;
